# Slim runtime deps for the hosted decision-memory service (service/app.py).
# Deliberately excludes the dev stack (redis, mongo, flask-cors, dashboard).
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
neo4j>=5.20
openai>=1.40
httpx>=0.27
